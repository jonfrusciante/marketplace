// import { GET_XSRF_TOKEN } from '../../actions/types';

// export default (state = {}, { type, payload = {} }) => {
// 	switch (type) {
// 		case GET_XSRF_TOKEN:
// 			return { ...state, payload };
// 		default:
// 			return state;
// 	}
// };
